{% extends 'front/template.html.twig' %}

{% block title %}{{ parent() }} - Commander{% endblock %}

{% block body %}
    <nav class="navbar navbar-expand-lg navbar-light">
        <a class="navbar-brand" href="{{ path('front_liste_pizza_index') }}">AppPizza... <em style="font-size: 80%;">Les pizzas à la mode du pilat</em></a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ml-auto">

                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('front_commande_pizza_recapitulatif') }}">
                        <div class="btn btn-outline-primary">
                            Panier :
                            <span id="cart-items">
                            {% if app.request.cookies.get('nbPizzas') %}
                                {{ app.request.cookies.get('nbPizzas') }}
                            {% else %}
                                vide
                            {% endif %}
                        </span>
                            <i class="fa fa-shopping-basket" aria-hidden="true"></i>
                        </div>
                    </a>
                </li>
            </ul>
        </div>
    </nav>
    <div class="container">
        <div class="row mt-3">
            <div class="col-12 col-sm-12 col-md-12 col-lg-12 col-xl-12">
                <h1>Commande terminée</h1>
                <div class="jumbotron">
                    <h1 class="display-4" style="font-size: 2.5rem">Félicitation !</h1>
                    <p class="lead">Votre commande a bien été prise en compte et nous vous en remercions. Retrouvez notre livreur devant votre porte dans peu de temps pour savourer votre délicieuse pizza à la mode du pilat.</p>
                </div>
            </div>
        </div>
        <div class="row mt-3">
            <div class="col-12">

            </div>
        </div>
    </div>
{% endblock %}